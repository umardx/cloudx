version: '3.3'
services:
    cloudx:
        ports:
            - '3000:3000'
        volumes:
            - './downloads:/downloads'
            - './torrents:/torrents'
            - './config:/etc/cloud-torrent'
        image: boypt/cloud-torrent
        command: --auth username:password
